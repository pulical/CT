name: Deploy based on env
on:
    push: 
     branches: ["*"]

jobs: 
  test:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v3
     - name: running staging env
       run: |
          chmod +x github.sh
          bash github.sh 
 
